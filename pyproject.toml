[project]
name = "mystockhelper-deployment"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "Flask>=3.0,<4",
  "requests>=2.32,<3"
]

[tool.vercel]
entrypoint = "mystockhelper.api.index:app"
